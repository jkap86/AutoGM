appId: com.autogm.desktop
productName: AutoGM
copyright: Copyright © 2026
directories:
  output: dist
  buildResources: resources
files:
  - from: .
    filter:
      - package.json
      - app
mac:
  category: public.app-category.utilities
  target:
    - target: dmg
      arch:
        - x64
        - arm64
win:
  target:
    - target: nsis
      arch:
        - x64
linux:
  target:
    - target: AppImage
      arch:
        - x64
artifactName: ${productName}-${version}-${os}-${arch}.${ext}
publish: null
